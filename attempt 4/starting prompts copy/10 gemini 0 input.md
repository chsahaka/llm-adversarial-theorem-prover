what else should be in the grave yard? and can you give the starting point, keep
that separate as a prompt not a system instruction, so that i can use the same
prompt for when i restart and can use the data from the critic and provers and
why don't we tack on points or penates from the observer?
