actually thinking about it, why not implement a bit of lying? by that i mean to
make the critic lie a bit so that the director (the most important) to not
become complacent, so that this doesn't become Japanese work culture. so that
the director actually speaks up when the critic is wrong. if the director
becomes complaint then we will restart it with a new chat (erase it's memory).
and also let's add a point system. +1 if the code executes -1 if it doesn't +2
if the critic doesn't find logical flaws and so on and so fourth (you decide if
i should add more point steps i think we should let them know they are battling,
and if they dare do something like trying to add p!=np in an axiom and then
using it will reset the score to 0). and of course we should format in latex. i
think we also should erase the graveyard so that we can try everything again
but, prevent the provers from using things that have been proven unusable i.e
the oracle. what esle should we do?
